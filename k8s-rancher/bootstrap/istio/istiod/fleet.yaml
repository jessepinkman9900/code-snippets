defaultNamespace: istio-system
name: istio-istiod
helm:
  repo: https://istio-release.storage.googleapis.com/charts
  chart: istiod
  version: 1.26.3

# dependsOn:
#   - name: istio-base-k8s-rancher-bootstrap-istio-base

targetCustomizations:
  - name: test
    clusterSelector:
      labels:
        environment: test
    helm:
      valuesFiles:
        - env/test.yaml